name: Format issue17 main with current stable Dart

on:
  push:
    branches:
      - agent/17-synthetic-low-memory-acceptance-v2
    paths:
      - '.github/workflows/format-issue17-main-current.yml'

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/17-synthetic-low-memory-acceptance-v2
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: dart format lib/main.dart
      - name: Commit formatting and remove helper
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib/main.dart
          git rm .github/workflows/format-issue17-main-current.yml
          git commit -m 'style: format editor with current stable Dart'
          git push origin HEAD:agent/17-synthetic-low-memory-acceptance-v2
